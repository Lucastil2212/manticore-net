# Optional. Copy to `.env` only if you load it yourself — the app reads the
# process environment, not a dotenv file.
#
# MANTICORE_HOME is a local path, not a secret.

MANTICORE_HOME=$HOME/manticore-net-data
